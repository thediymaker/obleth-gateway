# Runtime dependencies for the compressor sidecar.
# The build-time fetch dep (huggingface_hub) is NOT listed here; it is only
# needed by fetch_model.py to download the pre-built ONNX artefact at image
# build time. onnxruntime is the CPU wheel; model.py pins the CPUExecutionProvider.
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
onnxruntime>=1.18,<2.0
tokenizers>=0.19,<1.0
numpy>=1.26,<3.0
pydantic>=2.7,<3.0

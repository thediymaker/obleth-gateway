apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: obleth-prometheus
    access: proxy
    # In-network address of the observability-profile Prometheus container.
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 5s

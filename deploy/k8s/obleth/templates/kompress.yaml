{{- if .Values.kompress.enabled }}
# Optional neural prose compression sidecar. Serves POST /score + GET /health
# on port 8080. The obleth data plane calls it when OBLETH_KOMPRESS_URL is
# set; if absent or unreachable the gateway falls back to a built-in heuristic
# (fail-open). Enable via kompress.enabled=true in your values override; the
# image bakes a ~600 MB ONNX model so builds are slow.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-kompress
  labels: {{- include "obleth.labels" . | nindent 4 }}
spec:
  {{- if not .Values.kompress.autoscaling.enabled }}
  replicas: {{ .Values.kompress.replicas }}
  {{- end }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-kompress
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-kompress
    spec:
      securityContext: {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: kompress
          image: {{ .Values.kompress.image.repository }}:{{ .Values.kompress.image.tag | default (printf "v%s" .Chart.AppVersion) }}
          imagePullPolicy: {{ .Values.kompress.image.pullPolicy }}
          securityContext: {{- toYaml .Values.securityContext | nindent 12 }}
          ports:
            - { name: http, containerPort: 8080 }
          readinessProbe:
            httpGet: { path: /health, port: http }
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: http }
            initialDelaySeconds: 60
            periodSeconds: 30
          resources: {{- toYaml .Values.kompress.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-kompress
  labels: {{- include "obleth.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ .Release.Name }}-kompress
  ports:
    - { name: http, port: 8080, targetPort: http }
{{- if .Values.kompress.autoscaling.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-kompress
  labels: {{- include "obleth.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-kompress
  minReplicas: {{ .Values.kompress.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.kompress.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.kompress.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
{{- end }}

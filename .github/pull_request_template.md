## Summary

- what problem this PR solves
- what changed

## Contributor certificate

- [ ] I have read `CONTRIBUTOR_CERTIFICATE.md`, I have the right to submit this contribution, and I agree it may be incorporated into this project under the repository license terms.

## Validation

- [ ] `bash scripts/check-versions.sh` (if versions changed)
- [ ] `cargo clippy --workspace --all-targets` in `obleth/` (if Rust changed)
- [ ] `cargo test --workspace` in `obleth/` (if Rust behavior changed)
- [ ] `cargo build --release` in `benchmark-backend/` (if benchmark backend changed)
- [ ] `npm run build` in `control-plane/` (if control plane changed)

## Risk

- config change required: yes / no
- migration required: yes / no
- breaking change: yes / no

## Screenshots

Attach screenshots for UI changes.